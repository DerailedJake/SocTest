<div class="container">
  <div class="row row-cols-2 row-cols-sm-2 row-cols-md-3 g-3">
    <% stories.each do |story| %>
      <%= render partial: 'stories/story', locals: { author: author, story: story } %>
    <% end %>
  </div>
</div>
