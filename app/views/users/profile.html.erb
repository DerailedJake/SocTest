
<%= render partial: 'users/big', locals: { user: @user } %>

<hr class="featurette-divider">

<%= render partial: 'stories/stories', locals: { author: @user } %>

<hr class="featurette-divider">

<%= render partial: 'posts/posts', locals: { author: @user } %>

<hr class="featurette-divider">
