
<%= render partial: 'users/big', locals: { user: @user } %>

<hr class="featurette-divider">

<%= render partial: 'stories/stories', locals: { author: @user, stories: @stories } %>

<hr class="featurette-divider">

<%= render partial: 'posts/posts', locals: { author: @user, posts: @user.posts } %>

<hr class="featurette-divider">
