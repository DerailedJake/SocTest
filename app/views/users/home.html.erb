
<%= link_to "new posts", new_post_path %>
      <% if current_user.avatar.representable? %>
  <h2>
    SERgSERGsergserg
  </h2>
        <%= image_tag current_user.avatar.representation(resize_to_limit: [100, 100]) %>
      <% else %>
  <h2>
    SERgSERGsergserg
  </h2>
        <%= link_to rails_blob_path(current_user.avatar, disposition: "attachment") do %>
          <%= image_tag "placeholder.png", alt: "Download file" %>
        <% end %>
      <% end %>
<%= render partial: 'posts/posts', locals: { author: current_user, posts: @posts } %>