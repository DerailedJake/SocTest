$("#post-list").html("<%= escape_javascript(render partial: 'posts/posts', locals: { author: current_user, posts: @posts } ) %>")[0].scrollIntoView();
$("#post-pagination").remove()
$("#post-pagination").html("<%= escape_javascript(paginate @posts, window: 2, remote: true) %>")
