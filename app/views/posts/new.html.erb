<h2>New Post</h2>

<%= form_with model: @post do |f| %>
  <div class="field">
    <%= f.label :body, "Post content:" %><br />
    <%= f.text_area :body %>
  </div>

  <div class="actions">
    <%= f.submit "Create" %>
  </div>
<% end %>
